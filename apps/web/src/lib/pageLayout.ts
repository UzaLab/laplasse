/** Aligné sur la navbar — largeur de page standard (catalogue, checkout…) */
export const PAGE_CONTAINER = 'max-w-7xl mx-auto page-gutter-x'

/** Formulaires & wizards (inscription établissement, création boutique…) */
export const PUBLIC_NARROW = 'max-w-2xl mx-auto page-gutter-x'

/** Pages éditoriales (CGU, contact, privacy…) */
export const PUBLIC_CONTENT = 'max-w-3xl mx-auto page-gutter-x'

/** Cartes auth centrées (login, register) */
export const PUBLIC_AUTH = 'w-full max-w-md mx-auto px-4'
